name: test

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: jirutka/setup-alpine@v1
        with:
          branch: v3.15

      - name: Install wget
        run: apk add --no-cache wget
        shell: alpine.sh --root {0}

    
